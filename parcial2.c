#include <stdio.h>

int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    
    // Imprimir elementos del arreglo
    printf("Elementos del arreglo: ");
    for(int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }
    
    // Tu otro bucle iría aquí
    /*
    for (int i = 0; i < count; i++) {
        // código
    }
    */
   
    return 0;
}
